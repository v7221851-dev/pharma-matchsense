## 🛠️ Сводка по финальному коду и структуре проекта

Ваш проект теперь состоит из двух мощных и независимых скриптов, работающих в тандеме, а также специальной папки для входящих файлов.

| Файл / Папка | Назначение |
| :--- | :--- |
| **`preprocessing_script.py`** | **АЛГОРИТМ ПОДГОТОВКИ:** Автоматическая загрузка, маппинг колонок (по синонимам), очистка сырых наименований и стандартизация количества. |
| **`matching_script.py`** | **АЛГОРИТМ СОПОСТАВЛЕНИЯ:** Основная логика мэтчинга (МНН + Дозировка), нечеткий поиск и экспорт финального отчета. |
| **`register_ls.csv`** | **ЭТАЛОН:** Ваш реестр лекарственных средств. |
| **`new_purchases/`** | **ВХОДНАЯ ПАПКА:** Папка для хранения сырых файлов заявок (указана в конфигурации `preprocessing_script.py`). |
| **`purchase_input.csv`** | **ПРОМЕЖУТОЧНЫЙ ФАЙЛ:** Результат работы `preprocessing_script.py`, который читает `matching_script.py`. |

### 🚀 Последовательность запуска

1.  Поместите сырой файл заявки (например, `заявка.xlsx`) в папку **`new_purchases`**.
2.  **Подготовка:** Запустите `preprocessing_script.py`.
    ```bash
    (venv) python preprocessing_script.py
    ```
3.  **Мэтчинг:** Запустите `matching_script.py`.
    ```bash
    (venv) python matching_script.py
    ```

-----

## 🛡️ Рекомендации по сохранению и защите проекта (Git/GitHub)

Для защиты кода от случайных правок, удалений и отказов оборудования используйте **Git** и **GitHub**.

### 1\. Локальная защита (Git)

  * **Инициализация:** В VS Code на вкладке **Source Control** нажмите **Initialize Repository**.
  * **Коммит (Commit):** Фиксируйте состояние кода после каждой успешной доработки (вводите описание, например, "Рабочая версия с автоматическим маппингом"). Это позволяет "откатиться" к предыдущей версии.

### 2\. Облачная защита (GitHub)

  * **Отправка (Push):** На вкладке **Source Control** нажмите **Publish Branch** (Опубликовать ветку).
  * **Результат:** Ваш код хранится на серверах GitHub (бесплатная резервная копия), защищенная от любых локальных проблем.

-----

## 🌐 Рекомендации по созданию внешнего интерфейса (Streamlit)

Для создания внешнего интерфейса, чтобы не работать с командной строкой, используйте Python-фреймворк **Streamlit**.

### 1\. Почему Streamlit?

  * **Простота:** Весь интерфейс создается на чистом Python (минимум кода).
  * **Интеграция:** Идеально подходит для ваших скриптов по обработке данных.

### 2\. Передача приложения (Развертывание)

| Метод | Что это? | Плюсы | Минусы/Рекомендация |
| :--- | :--- | :--- | :--- |
| **Streamlit Community Cloud** | Сервис для размещения Streamlit-приложений. | Самый быстрый способ; бесплатно; обновления моментальны. | Ссылка будет иметь вид `app-name.streamlit.app`. |
| **Универсальный хостинг (Render)** | Платформа для веб-сервисов. | Позволяет подключить **собственный домен** (`analiz-zakupok.ru`). | Требует чуть большей настройки, но дает профессиональную ссылку. |
| **Исполняемый файл (.exe)** | Сборка кода в автономный файл через PyInstaller. | Не требует установки Python у пользователя. | Файл очень большой, сложная настройка для Streamlit, не рекомендуется. |

### 3\. Запуск Streamlit

После установки библиотеки (`pip install streamlit`) и переноса логики в файл **`app.py`** вы запускаете приложение:

```bash
(venv) streamlit run app.py
```

Скрипт автоматически откроет интерфейс в вашем браузере.